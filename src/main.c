#include <stdio.h>
#include <app_info.h>
#include <file_operations.h>

const char* file = "alma";

int main(void) {
    //create_file(file);
    //delete_file(file);
    //read_to_file(file);
    return 0;
}